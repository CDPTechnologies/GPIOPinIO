This is the project dir for building a CDP application.
The application uses customer libraries by linking them in at
compile time.

Each library contains a builder object that is
able to create components from the library at run-time, depending
of what is specified in Application/Application.xml.
